services:
  ai:
    build:
      context: .
      dockerfile: ai/Dockerfile
    container_name: angelwetter-ai
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./model-data:/model
